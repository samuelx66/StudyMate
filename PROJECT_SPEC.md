## 第一阶段：核心音视频引擎与波形图原型

- 音频和视频无缝播放、毫秒级 Seek、变速不变调（0.5x ~ 2.0x 无杂音）。
- 音频峰值数据抽取与缓存算法（几百兆的大音频文件秒级加载波形）。
- 60fps 平滑播放指针跟踪。
- 界面设计：
  - 主波形图与次波形图
  - 音频、视频播放区
  - 播放控制区
  - 断句列表显示区
- 音视频解码方案（支持菜单与设置中自由切换三种模式）：
  1. 系统解码：纯原生的 AVFoundation/AVPlayer 硬件直通解码（0ms 瞬间秒跳、极低 CPU 占用、高保真变速不变调）。
  2. libmpv 解码：完全使用业界顶级 libmpv 动态库解码，通吃 MKV, WebM, AVI, FLV, TS, RMVB 等全格式及非标/受损 MP4。
  3. 混合解码（推荐默认）：智能自动优选机制（标准格式优先 AVFoundation 极速通道，遇到格式不支持或报错时自动无缝回退到 libmpv）。

## 第二阶段：断句与智能复读系统

- 集成终极双引擎断句系统（Silero VAD 毫秒级声学防吞音与抗 BGM 杂音 + Whisper/Speech.framework 词级语义对齐与自动生成台词）。
- 手动切句、合并句子、微调波形锚点。
- 单句复读模式
- SRT / LRC / 文本导入与时间轴自动生成断句
- 波形图分主波形图和次波形图
  - 主波形图显示整个播放文件的波形图，在上面显示每个断句的开始和结束，不用当前可视界面中全部显示所有的断句，显示3-5个断句的波形图即可，根据播放进度，实时匹配显示对应的3-5个波形图即可
  - 次波形图是对当前正在播放断句的波形图的放大，以方便微调波形锚点
- 字幕编辑区（位于视频画面播放区域与底部播放控制条之间）：
  - 双行输入框：原文、译文
  - 输入框焦点离开（失焦）或切换断句时自动即时持久化保存
- 断句列表双行区域显示：
  - 第一行：句子序号、起止时间戳、时长与难句收藏
  - 第二行：分为两个区域，左边显示原文（默认 Sentence #），右边显示译文